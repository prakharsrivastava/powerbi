 // Define UDF to convert array of strings to JSON array of objects
  val arrayToJsonUDF = udf((array: Seq[String]) => {
    array.map { item =>
      val Array(label, value) = item.split(":")
      s"""{"label":"$label","value":"$value"}"""
    }
  })
